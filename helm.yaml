mysql:
  rootPassword: "rootpwd"
  user: travellist_user
  password: "password"
  database: travellist

php:
  repository: "karllhughes/laravel-kubernetes"
  tag: "0.3.0"
  fpmEnabled: false

# Add to use Ingress/SSL
service:
  type: ClusterIP
  HTTPPort: 80

ingress:
  enabled: true
  domain: laravel-kubernetes.karllhughes.com
